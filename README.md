• This app demonstrates a privacy‑preserving chat experience using Apple’s on‑device LLM. The Chat​View​Model streams responses, updates UI state safely on the main actor, and uses a protocol for DI and testing. See reply​Message​Using​AI(for​Message:) for the streaming loop and reply​With​Delay​Of​Few​Seconds() for UX pacing.
